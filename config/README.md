# Configuration

Creation Developer uses Suite-managed settings and provider accounts. App-local
configuration belongs here only when it cannot be represented by the shared
Suite VFS-backed settings services.

